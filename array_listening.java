      /*
      --------------Array listening-----------
      // const arr = [1, 2, 3, 4, 5];
      -array list is the re from java.util package.
      -Unlike it can grow and shrink inside dynamically.

      -it allows more flexibilities operation
       like insertion, removal, and other operation in elements


      -key features of array list
      1.Dynamic sizing 
      2.Index access
      3.Type safety
      4.Order(maintain the insertion order)

      */

